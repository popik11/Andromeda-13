import { Window } from 'tgui-core/components';

export const TestWindow = () => {
  return (
    <Window width={450} height={445}>
      <Window.Content>
        {/* Здесь можно оставить пустое содержимое или добавить другие компоненты позже */}
      </Window.Content>
    </Window>
  );
};
